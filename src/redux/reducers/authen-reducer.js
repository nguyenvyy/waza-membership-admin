
const initState = {
    user: null,
    isLoggedIn: true,
    isLoadingUser: true,
}

export const authReducer = (state = initState, action) => {
    switch (action) {
        default:
            return state
    }
} 