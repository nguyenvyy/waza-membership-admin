
const initState = {
    user: null,
    isLoggedIn: false,
    isLoadingUser: true,
}

export const authReducer = (state = initState, action) => {
    switch (action) {
        default:
            return state
    }
} 